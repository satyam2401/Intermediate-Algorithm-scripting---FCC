function dropElements(arr, func) {
  // Drop them elements.
  var x=arr.length;
  for(var i=0;i<x;i++){
    if(func(arr[0])===true){
      break;
    }
   else{
      arr.shift();
    }
  }
  
  return arr;
}
